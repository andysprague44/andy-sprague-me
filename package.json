{
    "name": "bold-brush-monorepo",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm --prefix client run dev\" \"npm --prefix cms run develop\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
